export type Candidates = {
  fileName: string;
  fileUrl: string;
  score: string;
};

export type JobDescription = {
  id: string;
  fileName: string;
  blobName: string;
  createdAt: string;
  updatedAt: string;
};
